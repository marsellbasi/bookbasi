---
interface Props {
  eyebrow?: string
  title: string
  copy?: string
  id?: string
}

const {eyebrow, title, copy, id} = Astro.props
---

<div class="section-heading">
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2 id={id}>{title}</h2>
  {copy && <p>{copy}</p>}
</div>
